module(
    name = "chatshare",
    version = "0.1.0",
    compatibility_level = 1,
)

use_repo_rule(":vcpkg.bzl", "get_packages")

bazel_dep(name = "rules_cc", version = "0.1.1")
bazel_dep(name = "googletest", version = "1.17.0")

